package com.portfoliopilot.model.enums;

/** Mirrors {@code profiles.experience[].employmentType} and {@code jobAnalyses.job.employmentType}. */
public enum EmploymentType {
    INTERNSHIP,
    FULL_TIME,
    PART_TIME,
    FREELANCE,
    PROJECT
}
