package com.portfoliopilot.model.enums;

/** Mirrors {@code skillDictionary.category} and {@code profiles.skills[].category}. */
public enum SkillCategory {
    FRONTEND,
    BACKEND,
    DATABASE,
    DEVOPS,
    CLOUD,
    MOBILE,
    TESTING,
    DATA_AI,
    TOOLS,
    LANGUAGE,
    SOFT_SKILL,
    OTHER
}
